# Prompt recordings

Add these files here:

- `tap.mp3` - “Bop it”
- `swipe.mp3` - “Swipe it”
- `press.mp3` - “Press it”

Restart `go run .` after adding or replacing recordings because the files are embedded at startup.
